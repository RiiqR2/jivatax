export const INVALID_CREDENTIALS = "Correo o contraseña incorrectos.";
export const ACCESS_TOKEN_TYPE = "access" as const;
export const REFRESH_TOKEN_TYPE = "refresh" as const;
export const DEFAULT_ACCESS_TTL_SECONDS = 900;
export const DEFAULT_REFRESH_TTL_SECONDS = 604800;

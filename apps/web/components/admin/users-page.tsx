'use client';
import { FormEvent, useCallback, useEffect, useMemo, useState } from 'react'; import { usersApi, UserInput } from '../../lib/admin/users-api'; import type { Membership, MembershipStatus, OrganizationRole } from '../../lib/admin/types'; import { EmptyState, ErrorState, LoadingState, PageHeader, StatusBadge } from './ui';
const roles:Record<OrganizationRole,string>={owner:'Propietario',admin:'Administrador',accountant:'Contador',auditor:'Auditor',viewer:'Consulta'}; const blank:UserInput={email:'',firstName:'',lastName:'',role:'accountant'};
export function UsersPage(){const [users,setUsers]=useState<Membership[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[open,setOpen]=useState(false),[form,setForm]=useState(blank),[saving,setSaving]=useState(false),[formError,setFormError]=useState(''),[success,setSuccess]=useState(''),[search,setSearch]=useState(''),[role,setRole]=useState(''),[status,setStatus]=useState(''); const load=useCallback(async()=>{setLoading(true);setError('');try{setUsers(await usersApi.list())}catch(e){setError(e instanceof Error?e.message:'Error inesperado.')}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);const filtered=useMemo(()=>users.filter(m=>(!search||`${m.user.firstName} ${m.user.lastName} ${m.user.email}`.toLowerCase().includes(search.toLowerCase()))&&(!role||m.role===role)&&(!status||m.status===status)),[users,search,role,status]);
async function submit(e:FormEvent){e.preventDefault();if(!form.firstName.trim()||!form.lastName.trim()||!form.email.includes('@')){setFormError('Completa nombre, apellido y un correo válido.');return}setSaving(true);setFormError('');try{await usersApi.create(form);setOpen(false);setForm(blank);setSuccess('Invitación creada correctamente.');await load()}catch(e){setFormError(e instanceof Error?e.message:'No se pudo agregar el usuario.')}finally{setSaving(false)}}async function update(m:Membership,input:{role?:OrganizationRole;status?:MembershipStatus}){try{await usersApi.update(m.userId,input);setSuccess('Membresía actualizada correctamente.');await load()}catch(e){setError(e instanceof Error?e.message:'No se pudo actualizar.')}}
return <main className="page"><PageHeader title="Usuarios" description="Gestiona las membresías y roles de tu organización." action={<button className="button" onClick={()=>{setFormError('');setOpen(true)}}>Nuevo usuario</button>}/>{success&&<div className="alert success">{success}</div>}<div className="filters filters-three"><input aria-label="Buscar usuarios" placeholder="Buscar por nombre o correo" value={search} onChange={e=>setSearch(e.target.value)}/><select aria-label="Filtrar por rol" value={role} onChange={e=>setRole(e.target.value)}><option value="">Todos los roles</option>{Object.entries(roles).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select><select aria-label="Filtrar por estado" value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos los estados</option><option value="invited">Invitado</option><option value="active">Activo</option><option value="suspended">Suspendido</option></select></div><section className="card">{loading?<LoadingState/>:error?<ErrorState message={error} retry={()=>void load()}/>:filtered.length===0?<EmptyState title="No hay usuarios" action={<button className="button" onClick={()=>setOpen(true)}>Agregar usuario</button>}>Invita al primer colaborador de la organización.</EmptyState>:<div className="table-wrap"><table><thead><tr><th>Nombre completo</th><th>Correo</th><th>Rol</th><th>Estado</th><th>Incorporación</th><th>Acciones</th></tr></thead><tbody>{filtered.map(m=><tr key={m.id}><td className="strong">{m.user.firstName} {m.user.lastName}</td><td>{m.user.email}</td><td><select value={m.role} aria-label={`Rol de ${m.user.email}`} onChange={e=>void update(m,{role:e.target.value as OrganizationRole})}>{Object.entries(roles).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></td><td><StatusBadge status={m.status}/></td><td>{new Intl.DateTimeFormat('es-CL').format(new Date(m.joinedAt??m.createdAt))}</td><td><button className="text-button" onClick={()=>void update(m,{status:m.status==='suspended'?'active':'suspended'})}>{m.status==='suspended'?'Reactivar':'Suspender'}</button></td></tr>)}</tbody></table></div>}</section>{open&&<div className="overlay"><form className="modal" onSubmit={submit}><h2>Nuevo usuario</h2><p className="modal-intro">El usuario quedará invitado, sin credenciales de acceso.</p>{formError&&<div className="alert">{formError}</div>}<label className="field">Nombre<input maxLength={100} value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})}/></label><label className="field">Apellido<input maxLength={100} value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})}/></label><label className="field">Correo<input type="email" maxLength={255} value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="field">Rol<select value={form.role} onChange={e=>setForm({...form,role:e.target.value as OrganizationRole})}>{Object.entries(roles).map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label><div className="actions"><button type="button" className="button button-secondary" onClick={()=>setOpen(false)}>Cancelar</button><button className="button" disabled={saving}>{saving?'Agregando…':'Agregar usuario'}</button></div></form></div>}</main>}
